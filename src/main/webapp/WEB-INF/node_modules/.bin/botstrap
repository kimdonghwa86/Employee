#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/iojs" ]; then
  "$basedir/iojs"  "$basedir/../botstrap/index.js" "$@"
  ret=$?
else 
  iojs  "$basedir/../botstrap/index.js" "$@"
  ret=$?
fi
exit $ret
